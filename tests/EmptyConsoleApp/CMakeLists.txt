# This file was generated by Jucer2Reprojucer from EmptyConsoleApp.jucer

cmake_minimum_required(VERSION 3.4)


list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../cmake")
include(Reprojucer)


jucer_project_begin(
  PROJECT_FILE "${CMAKE_CURRENT_LIST_DIR}/EmptyConsoleApp.jucer"
  PROJECT_ID "wdEgLn"
)

jucer_project_settings(
  PROJECT_NAME "EmptyConsoleApp"
  PROJECT_VERSION "1.0.0"
  PROJECT_TYPE "Console Application"
  BUNDLE_IDENTIFIER "com.yourcompany.EmptyConsoleApp"
  BINARYDATACPP_SIZE_LIMIT "Default"
)

jucer_project_files("EmptyConsoleApp"
  x . . "main.cpp"
)

jucer_export_target(
  "Xcode (MacOSX)"
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Debug"
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Release"
)

jucer_export_target(
  "Visual Studio 2015"
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Debug"
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Release"
)

jucer_export_target(
  "Visual Studio 2013"
)

jucer_export_target_configuration(
  "Visual Studio 2013"
  NAME "Debug"
)

jucer_export_target_configuration(
  "Visual Studio 2013"
  NAME "Release"
)

jucer_export_target(
  "Linux Makefile"
)

jucer_export_target_configuration(
  "Linux Makefile"
  NAME "Debug"
)

jucer_export_target_configuration(
  "Linux Makefile"
  NAME "Release"
)

jucer_project_end()
